{
  "name": "@hop/plugin-paystack",
  "version": "1.0.0",
  "private": true,
  "description": "HOP payment plugin — Paystack gateway",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "hop-plugin": "./hop-plugin.json",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@hop/plugin-sdk": "workspace:*"
  },
  "devDependencies": {
    "@hop/tsconfig": "workspace:*",
    "rimraf": "^6.1.3",
    "typescript": "^5.4.0"
  }
}
